{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd .. && pnpm install",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD~1 HEAD -- . ../packages/shared/ 2>/dev/null || exit 1"
}
